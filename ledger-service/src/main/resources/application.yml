spring:
  config:
    import: "classpath:application-common.yml"
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/ledger,classpath:db/migration-technical
  application:
    name: ledger-service
